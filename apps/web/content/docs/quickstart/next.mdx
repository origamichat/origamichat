---
title: Next.js
description: Setup with Next.js.
---

## init

Use the `init` command to initialize configuration and dependencies for a new next project.

The `init` command installs dependencies, adds the `cn` util and configures CSS variables for the project.

```bash
npx @cossistant/cli@latest init
```

## Usage
